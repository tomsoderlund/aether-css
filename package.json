{
  "name": "aether-css-framework",
  "description": "Simple, Interactive, Fun, Beautiful CSS system",
  "version": "1.1.0",
  "author": "Tom Söderlund <tom@tomsoderlund.com>",
  "main": "aether.css",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "prepare": "rm -rf dist && mkdir -p dist/icons && css-minify -f aether.css && mv css-dist/* dist/ && cp icons/* dist/icons/"
  },
  "devDependencies": {
    "css-minify": "^1.1.0"
  }
}
